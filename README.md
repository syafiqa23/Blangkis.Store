
# Blangkis Store

Blangkis Store is a CodeIgniter 4 application. The production deployment procedure for Ubuntu with Nginx and PHP-FPM is documented in [DEPLOYMENT.md](DEPLOYMENT.md).

For local development, copy `.env.example` to `.env`, set the local database values, and run:

```bash
composer install
php spark serve
```
